name: web-native
version: 1.0.0
description: "Local web content extraction via trafilatura (no API key, no external service)."
author: NousResearch (OTG)
kind: backend
provides_web_providers:
  - native
